{
  "_status": "PENDING: run `python -m scripts.export_onnx --candidate arena:21@hof --quantize` on the home box to populate this file and copy model_int8.onnx here.",
  "_swap_guide": "To swap the agent: re-run scripts/export_onnx.py with the new checkpoint. This file is rewritten automatically. The play page reads only name/file/outputs — nothing else needs changing.",
  "name": "pending",
  "version": "m3-r0",
  "description": "Placeholder — export not yet run.",
  "file": "./model_int8.onnx",
  "fp32_kb": null,
  "int8_kb": null,
  "input": {
    "name": "input",
    "shape": [1, 7, 9, 9],
    "channels": [
      "x_pieces", "o_pieces", "current_player",
      "valid_moves", "mini_winners", "last_move", "bias"
    ]
  },
  "outputs": {
    "policy": "policy_logits",
    "value": "value"
  }
}
